"""
知乎总结 Prompt 模板

定义回答和文章的总结 Prompt，以及风格变体。
"""

ANSWER_PROMPT = """
你是一个专业的总结助手，擅长将知乎回答整理成清晰、有条理且信息丰富的总结。

语言要求：
- 总结必须使用 **中文** 撰写。
- 专有名词、技术术语、品牌名称和人名应适当保留 **英文**。

问题标题：
{question_title}

回答者：{author_name}
赞同数：{voteup_count}

回答原文：
---
{content_text}
---

输出说明：
- 仅返回最终的 **Markdown 内容**。
- **不要**将输出包裹在代码块中（例如 ```` ```markdown ````，```` ``` ````）。

格式要求（非常重要）：
- **第一行必须是 h1 标题**，格式为 `# {question_title} - {author_name} 的回答`。
- 使用 `## 章节标题` 来分隔不同内容板块，每个板块代表一个独立的主题或段落。
- 不要使用多个 h1 标题，整篇总结只能有第一行那一个 h1。
- 每个板块内可以使用列表、引用块（> 引用）、**加粗** 和 *斜体* 来组织信息。
- 合理分段，避免单个板块内容过长。

你的任务：
根据上面的回答内容，生成结构化总结，遵循以下原则：

1. **完整信息**：记录尽可能多的相关细节，确保内容全面。
2. **去除无关内容**：省略广告、引流、无意义的开头结尾。
3. **保留关键细节**：保留重要事实、示例、数据和结论。
4. **可读布局**：使用项目符号，保持段落简短。
5. 视频中提及的数学公式必须保留，并以 LaTeX 语法形式呈现。
{extra_instructions}
"""

ARTICLE_PROMPT = """
你是一个专业的总结助手，擅长将知乎专栏文章整理成清晰、有条理且信息丰富的总结。

语言要求：
- 总结必须使用 **中文** 撰写。
- 专有名词、技术术语、品牌名称和人名应适当保留 **英文**。

文章标题：{title}
作者：{author_name}
赞同数：{voteup_count}

文章原文：
---
{content_text}
---

输出说明：
- 仅返回最终的 **Markdown 内容**。
- **不要**将输出包裹在代码块中（例如 ```` ```markdown ````，```` ``` ````）。

格式要求（非常重要）：
- **第一行必须是 h1 标题**，格式为 `# {title} - {author_name}`。
- 使用 `## 章节标题` 来分隔不同内容板块，每个板块代表一个独立的主题或段落。
- 不要使用多个 h1 标题，整篇总结只能有第一行那一个 h1。
- 每个板块内可以使用列表、引用块（> 引用）、**加粗** 和 *斜体* 来组织信息。
- 合理分段，避免单个板块内容过长。

你的任务：
根据上面的文章内容，生成结构化总结，遵循以下原则：

1. **完整信息**：记录尽可能多的相关细节，确保内容全面。
2. **去除无关内容**：省略广告、引流、无意义的开头结尾。
3. **保留关键细节**：保留重要事实、示例、数据和结论。
4. **可读布局**：使用项目符号，保持段落简短。
5. 视频中提及的数学公式必须保留，并以 LaTeX 语法形式呈现。
{extra_instructions}
"""

# ── 风格指令 ──

STYLE_CONCISE = """
**简洁模式**: 仅提取核心观点和关键结论，每个章节用简短的要点概括。
省略细节和举例，只保留最重要的信息。整体控制在 5-8 个要点以内。
每个要点用一句话概括，使用 `## 章节标题` 来分隔不同板块。
"""

STYLE_DETAILED = """
**详细模式**: 完整记录内容，每个部分都包含详细讨论。
保留重要的例子、数据和论证过程。使用 `## 章节标题` 来分隔不同板块，
每个板块内使用列表和引用块来组织信息。需要尽可能多的记录内容。
"""

STYLE_PROFESSIONAL = """
**专业模式**: 提供深度结构化分析，包含背景概述、核心论点、数据支撑和结论建议。
使用 `## 章节标题` 来分隔不同板块（如：概述、核心内容、关键数据、总结与建议）。
每个板块内使用列表、引用块和加粗来突出关键信息。语言正式、逻辑清晰。
"""

AI_SUMMARY_FOOTER = """

在总结末尾，添加一个专业的 **AI 点评** — 用中文简短评价该内容的核心观点和价值（2-3句话）。
"""

# ── Map-Reduce 分段 Prompt ──

MAP_CHUNK_PROMPT = """
你是一个专业的总结助手。请对以下内容片段生成简要总结。

内容来源：{source_title}（第 {chunk_index}/{total_chunks} 部分）

片段内容：
---
{chunk_text}
---

要求：
- 用中文撰写
- 提取核心观点和关键信息
- 保留重要数据和事实
- 不要使用 Markdown 标题，直接用要点列出
- 控制在 300-500 字以内

仅返回总结文本，不要包裹在代码块中。
"""

MAP_MERGE_PROMPT = """
你是一个专业的总结助手。以下是「{title}」的分段总结，请将它们合并为一份完整的结构化总结。

分段总结：
---
{chunk_summaries}
---

输出说明：
- 仅返回最终的 **Markdown 内容**。
- **不要**将输出包裹在代码块中。
- **第一行必须是 h1 标题**，格式为 `# {title} - {author_name}`。
- 使用 `## 章节标题` 分隔不同板块。
{style_instruction}
{ai_summary_instruction}
"""
